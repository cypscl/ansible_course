---
- name: Install & Configure Apache2
  hosts: all

  tasks:
    - name: Apt update
      ansible.builtin.apt:
        update_cache: true

    - name: Install Apache2
      ansible.builtin.apt:
        name: apache2
        state: present
